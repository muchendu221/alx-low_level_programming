#include "dog.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * free_dog - frees the memory
 *@d memory to be freed
 *
 * Return: Always return 0
 */
void free_dog(dog_t *d)
{
	if (d !== NULL)
	{
	free(d->name);
	free(d->owner);
	free(d);
	}
}
